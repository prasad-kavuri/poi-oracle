# POI Oracle v3.0 Configuration
# ================================

# ===== LLM Configuration =====

# Enable LLM integration (set to 'true' to use real AI)
NEXT_PUBLIC_USE_LLM=false

# LLM Provider: 'ollama' (local, free), 'openai' (cloud), or 'simulate' (demo mode)
NEXT_PUBLIC_LLM_PROVIDER=ollama

# ===== Ollama Configuration (Recommended) =====
# Install: curl -fsSL https://ollama.ai/install.sh | sh
# Pull model: ollama pull llama3.2
# Start: ollama serve

NEXT_PUBLIC_OLLAMA_URL=http://localhost:11434/v1
NEXT_PUBLIC_OLLAMA_MODEL=llama3.2

# Recommended models:
# - llama3.2      (3B params, fast, good quality)
# - llama3.2:1b   (1B params, very fast)
# - mistral       (7B params, excellent reasoning)
# - phi3          (3B params, Microsoft model)

# ===== OpenAI Configuration (Alternative) =====
# Get API key from: https://platform.openai.com/api-keys
# Cost: ~$0.002 per query with GPT-3.5

# NEXT_PUBLIC_OPENAI_API_KEY=sk-your-openai-api-key-here

# ===== Map Configuration =====
# Currently using OpenStreetMap with CartoDB Dark tiles (free, no API key)
# Future: Add Ola Maps API integration

# OLA_MAPS_API_KEY=your-ola-maps-key

# ===== Development =====
NODE_ENV=development

# ===== Quick Start Guide =====
# 
# DEMO MODE (no setup):
#   Just run: npm run dev
#   App works with simulated AI reasoning
#
# WITH OLLAMA (recommended):
#   1. Install Ollama: curl -fsSL https://ollama.ai/install.sh | sh
#   2. Pull model: ollama pull llama3.2
#   3. Start Ollama: ollama serve
#   4. Set NEXT_PUBLIC_USE_LLM=true
#   5. Run: npm run dev
#
# WITH OPENAI:
#   1. Get API key from OpenAI
#   2. Set NEXT_PUBLIC_USE_LLM=true
#   3. Set NEXT_PUBLIC_LLM_PROVIDER=openai
#   4. Set NEXT_PUBLIC_OPENAI_API_KEY=sk-...
#   5. Run: npm run dev
